# Copy to .env.local. Obtain values from the RUNNING LOCAL Supabase CLI.
# Do not use a remote URL or credentials during Phase 1 validation.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# No service-role/secret key is needed by this application foundation.
